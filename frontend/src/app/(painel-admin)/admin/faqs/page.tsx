'use client'

import { useState } from 'react'

import { ContractState, PendingActionFeedback, usePendingContractActions } from '@/components/feedback/contract-state'
import { Button } from '@/components/ui/button'
import { Dialog, DialogContent, DialogDescription, DialogFooter, DialogHeader, DialogTitle } from '@/components/ui/dialog'
import { Input } from '@/components/ui/input'
import { Label } from '@/components/ui/label'
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select'
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from '@/components/ui/table'
import { Textarea } from '@/components/ui/textarea'
import { PENDING_BACKEND_CONTRACTS } from '@/lib/api-contract'

export default function AdminFaqPage() {
  const [editorOpen, setEditorOpen] = useState(false)
  const [editing, setEditing] = useState(false)
  const { error, attemptedAction, runPendingAction } = usePendingContractActions(PENDING_BACKEND_CONTRACTS.notices)
  return <section className="space-y-6"><div className="flex justify-between gap-4"><div><h1 className="text-2xl font-bold">FAQs</h1><p className="text-sm text-gray-600">Perguntas, categorias, respostas e ordenação.</p></div><Button type="button" onClick={() => { setEditing(false); setEditorOpen(true) }}>Nova FAQ</Button></div><ContractState error={error} /><PendingActionFeedback attemptedAction={attemptedAction} /><div className="grid gap-3 rounded-lg border bg-white p-4 md:grid-cols-3"><Input placeholder="Buscar pergunta" /><Select defaultValue="TODAS"><SelectTrigger><SelectValue /></SelectTrigger><SelectContent>{['TODAS', 'GERAL', 'CONTA', 'PAGAMENTOS', 'SEGURANCA', 'ANUNCIOS'].map((item) => <SelectItem key={item} value={item}>{item}</SelectItem>)}</SelectContent></Select><Button type="button" variant="outline" onClick={() => runPendingAction('Filtrar FAQs')}>Aplicar filtros</Button></div><div className="overflow-x-auto rounded-lg border bg-white"><Table><TableHeader><TableRow><TableHead>Pergunta</TableHead><TableHead>Categoria</TableHead><TableHead>Resposta</TableHead><TableHead>Ordem</TableHead><TableHead>Ações</TableHead></TableRow></TableHeader><TableBody><TableRow><TableCell colSpan={5}><ContractState error={error} compact /><div className="mt-3 flex flex-wrap gap-2"><Button type="button" variant="outline" onClick={() => { setEditing(true); setEditorOpen(true) }}>Editar</Button><Button type="button" variant="outline" onClick={() => runPendingAction('Reordenar FAQ para cima')}>Mover para cima</Button><Button type="button" variant="outline" onClick={() => runPendingAction('Reordenar FAQ para baixo')}>Mover para baixo</Button><Button type="button" variant="destructive" onClick={() => runPendingAction('Excluir FAQ')}>Excluir</Button></div></TableCell></TableRow></TableBody></Table></div><Dialog open={editorOpen} onOpenChange={setEditorOpen}><DialogContent><DialogHeader><DialogTitle>{editing ? 'Editar FAQ' : 'Nova FAQ'}</DialogTitle><DialogDescription>Cadastre pergunta, resposta, categoria e ordem de exibição.</DialogDescription></DialogHeader><div className="space-y-4"><div className="space-y-2"><Label htmlFor="faq-question">Pergunta</Label><Input id="faq-question" /></div><div className="space-y-2"><Label htmlFor="faq-answer">Resposta</Label><Textarea id="faq-answer" rows={6} /></div><div className="space-y-2"><Label>Categoria</Label><Select defaultValue="GERAL"><SelectTrigger><SelectValue /></SelectTrigger><SelectContent>{['GERAL', 'CONTA', 'PAGAMENTOS', 'SEGURANCA', 'ANUNCIOS'].map((item) => <SelectItem key={item} value={item}>{item}</SelectItem>)}</SelectContent></Select></div><div className="space-y-2"><Label htmlFor="faq-order">Ordem</Label><Input id="faq-order" type="number" min={0} /></div><PendingActionFeedback attemptedAction={attemptedAction} /></div><DialogFooter><Button type="button" variant="outline" onClick={() => setEditorOpen(false)}>Cancelar</Button><Button type="button" onClick={() => runPendingAction(editing ? 'Salvar FAQ' : 'Criar FAQ')}>Salvar</Button></DialogFooter></DialogContent></Dialog></section>
}
