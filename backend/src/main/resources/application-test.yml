spring:
  config:
    activate:
      on-profile: test

app:
  auth:
    hml-code-vault-enabled: true
  outbox:
    email:
      enabled: false
      encryption-key: YWFhYWFhYWFhYWFhYWFhYQ==
      recipient-mode: CAPTURE
      capture-address: capture@test.invalid
  env: test
  canonical-domain: http://localhost

efi:
  pix:
    enabled: false
    environment: homologacao
    base-url: https://pix-h.api.efipay.com.br

server:
  port: 0
