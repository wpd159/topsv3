spring:
  main:
    web-application-type: none
  task:
    scheduling:
      enabled: false

app:
  outbox:
    email:
      enabled: false
  storage:
    r2:
      enabled: false
  migracao:
    integral:
      enabled: false
      pacote:
      modo:
      tamanho-lote: 500

efi:
  pix:
    enabled: false
    reconciliation-enabled: false
    webhook-registration-enabled: false
